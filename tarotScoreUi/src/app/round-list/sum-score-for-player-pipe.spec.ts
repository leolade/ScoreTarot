import { SumScoreForPlayerPipe } from './sum-score-for-player-pipe';

describe('SumScoreForPlayerPipe', () => {
  it('create an instance', () => {
    const pipe = new SumScoreForPlayerPipe();
    expect(pipe).toBeTruthy();
  });
});
